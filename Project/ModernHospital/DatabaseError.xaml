<UserControl x:Class="LaboratoireProgrammation.Project.ModernHospital.DatabaseError"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LaboratoireProgrammation.Project.ModernHospital"
             mc:Ignorable="d"
             d:DesignHeight="auto" d:DesignWidth="auto" Background="Transparent">
    <Grid>
        <StackPanel>
            <TextBlock Text="[E-1] DB.LOAD.FAIL [E-1]" Foreground="White" FontSize="96" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,100"/>
            
            <Button Height="140" Foreground="White" FontSize="96" FontWeight="Bold" Background="#FF5C41" BorderBrush="Transparent"  BorderThickness="2" Click="OnCloseApp">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#FF634E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Button.Resources>
                    <SolidColorBrush x:Key="HoverBrush" Color="#FF634E"/>
                </Button.Resources>
                CLOSE APP
            </Button>
        </StackPanel>

    </Grid>
</UserControl>
