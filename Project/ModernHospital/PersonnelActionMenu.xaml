<Window x:Class="LaboratoireProgrammation.Project.ModernHospital.PersonnelActionMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Actions Employé" Height="180" Width="200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" SizeToContent="WidthAndHeight" WindowStartupLocation="Manual">
    <Window.Resources>
        <Style x:Key="MenuBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#f0f0f5" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Margin="2">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2a2a38" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1c1c22" CornerRadius="8" BorderBrush="#2a2a38" BorderThickness="1">
        <StackPanel Margin="4">
            <TextBlock Text="CHANGER STATUT EMPLOYE" Foreground="#8888aa" FontSize="10" FontWeight="Bold"
                       Margin="12,8,12,4" />

            <Button Style="{StaticResource MenuBtn}" Click="Status_Click" Tag="En poste">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="[ 1 ]" Margin="0,0,10,0" />
                    <TextBlock Text="En poste" Foreground="#71DE46" />
                </StackPanel>
            </Button>

            <Button Style="{StaticResource MenuBtn}" Click="Status_Click" Tag="En pause">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="[ 2 ]" Margin="0,0,10,0" />
                    <TextBlock Text="En pause" Foreground="#EF8532" />
                </StackPanel>
            </Button>

            <Button Style="{StaticResource MenuBtn}" Click="Status_Click" Tag="Absent">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="[ 3 ]" Margin="0,0,10,0" />
                    <TextBlock Text="Absent" Foreground="#3E96B7" />
                </StackPanel>
            </Button>

            <Separator Background="#2a2a38" Margin="8,4" />

            <Button Style="{StaticResource MenuBtn}" Click="Licencier_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="[ - ]" Margin="0,0,10,0" />
                    <TextBlock Text="Licencier" Foreground="#ff4d6a" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</Window>