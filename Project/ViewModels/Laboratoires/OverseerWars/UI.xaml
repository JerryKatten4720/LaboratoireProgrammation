<UserControl x:Class="LaboratoireProgrammation.Project.ViewModels.Laboratoires.OverseerWars.UI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wrappers="clr-namespace:LaboratoireProgrammation.Assets.shaders.wrappers"
             xmlns:viewModels1="clr-namespace:LaboratoireProgrammation.Project.ViewModels.Menu"
             xmlns:overseerWars="clr-namespace:LaboratoireProgrammation.Project.ViewModels.Laboratoires.OverseerWars"

             mc:Ignorable="d" Background="Transparent">
    
    <UserControl.Resources>
        
        <Style TargetType="Label" x:Key="MyLabel">
            <Setter Property="Foreground" Value="WhiteSmoke" />
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/fonts/#ubuntu mono" />
            <Setter Property="Margin" Value="0,0,40,0" />
        </Style>
        
        <Style TargetType="Button" x:Key="NavigationButton">
            <Setter Property="Foreground" Value="WhiteSmoke" />
            <Setter Property="Background" Value="#78000000" />
            <Setter Property="BorderBrush" Value="#B4000000" />
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="FontSize" Value="32" />
            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/fonts/#ubuntu mono" />
            <Setter Property="Margin" Value="0,0,20,0" />
            <Setter Property="Padding" Value="20,10,20,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D7D7D7" />
                                <Setter Property="Foreground" Value="Black" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        
    </UserControl.Resources>
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="60" /> <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Background="#4B000000">
            <Label Style="{StaticResource MyLabel}" Margin="20,0,20,0">Power : 200/450</Label>
            <Label Style="{StaticResource MyLabel}">Food : 200/450</Label>
            <Label Style="{StaticResource MyLabel}">Wood : 200/450</Label>
            <Label Style="{StaticResource MyLabel}">Metal : 200/300</Label>
            <Label Style="{StaticResource MyLabel}">Caps : 200/300</Label>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Background="Transparent" Margin="0,20,0,0">
            <Button Style="{StaticResource NavigationButton}" Margin="20,0,20,0">Abri</Button>
            <Button Style="{StaticResource NavigationButton}">Terres désolées</Button>
        </StackPanel>
        
        <overseerWars:Wastelands Grid.Row="2"
                                 Visibility="Visible" 
                                 HorizontalAlignment="Center" 
                                 VerticalAlignment="Center" Margin="20"
        />
        
    </Grid>
    
</UserControl>

